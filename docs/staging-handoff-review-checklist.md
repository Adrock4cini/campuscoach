Review checklist:

- workflow checkout pins the reviewed checkout action;
- checkout ref remains the operator-supplied candidate SHA;
- `persist-credentials: false` is present;
- exact HEAD check remains;
- porcelain clean-tree check remains;
- replay runner is unchanged;
- migration SQL is unchanged.
