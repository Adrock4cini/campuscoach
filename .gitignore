# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# This repository is npm-only (see packageManager in package.json and CI).
# Ignore lockfiles generated when another package manager is run locally or by tooling.
bun.lock
bun.lockb
# .env holds only public browser config (project URL + publishable key) and MUST
# stay tracked: the hosted production build is made from the git snapshot, and
# an untracked .env ships a bundle with undefined Supabase config.
.env.local
.env.*.local
*.local
*.tsbuildinfo
test-results/
playwright-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
